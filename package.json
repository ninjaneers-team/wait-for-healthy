{
    "name": "wait-for-healthy",
    "version": "0.1.0",
    "scripts": {
        "test": "./node_modules/.bin/bats wait-for-healthy.bats"
    },
    "dependencies": {
        "bats": "^0.4.2"
    }
}